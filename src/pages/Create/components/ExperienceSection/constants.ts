import { RU } from "@/locales";

const {
  CREATE: {
    EXPERIENCE_LEVELS: {
      NOVICE,
      INTERMEDIATE,
      ADVANCED
    }
  }
} = RU;

/** Константы для уровней опыта */
export const EXPERIENCE_LEVELS = [
  NOVICE,
  INTERMEDIATE,
  ADVANCED
];
